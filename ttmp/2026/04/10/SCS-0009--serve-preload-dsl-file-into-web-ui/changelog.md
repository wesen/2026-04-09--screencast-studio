# Changelog

## 2026-04-10

- Created ticket `SCS-0009` for serve-time DSL preload support.
- Added a small implementation guide for CLI preload, bootstrap API data, and frontend hydration.
- Started the ticket diary for the implementation work.
- Implemented serve-time DSL preload in commit `f515188` (`serve: preload setup file into web ui`).
- Added backend health/bootstrap coverage and validated the UI with a production `pnpm --dir ui build`.
