# Tasks

## TODO

- [x] Add tasks here

- [x] Phase 0.1: Create pkg/media/types.go with runtime interfaces (PreviewRuntime, RecordingRuntime, PreviewSession, RecordingSession) and event/log types. See Section 7.2 of the analysis doc for interface signatures.
- [x] Phase 0.2: Create pkg/media/gst/ directory with placeholder files (preview.go, recording.go, pipeline.go, audio.go, bus.go). These start empty but establish the package structure.
- [x] Phase 0.3: Create pkg/media/ffmpeg/ directory. Move the current FFmpeg-specific code from pkg/recording/ffmpeg.go and internal/web/preview_runner.go into adapter implementations behind the new runtime interfaces. No behavior change — the existing code just gets wrapped behind the interface seam.
- [x] Phase 0.4: Wire the runtime interfaces into Application (pkg/app/application.go) and PreviewManager (internal/web/preview_manager.go). The Application struct gets a RecordingRuntime field; PreviewManager gets a PreviewRuntime field instead of the hardcoded FFmpegPreviewRunner. Default to FFmpeg implementations.
- [x] Phase 0.5: Run all existing tests. Verify the web UI still works identically. No behavior change is expected — this phase is purely structural. The FFmpeg implementation remains active behind the new interface.
- [x] Phase 1.1: Implement GStreamerPreviewRuntime in pkg/media/gst/preview.go. Build the preview pipeline programmatically: ximagesrc/v4l2src -> videoconvert -> videoscale -> capsfilter(width=640) -> videorate(5fps) -> jpegenc -> appsink. Based on the validated experiment in scripts/06-go-gst-preview-pipeline/.
- [x] Phase 1.2: Wire the appsink NewSampleFunc callback to push JPEG frame bytes into the PreviewManager.storePreviewFrame() method. This replaces the current MJPEG stdout parsing in preview_runner.go with direct buffer access.
- [x] Phase 1.3: Implement bus message handling in pkg/media/gst/bus.go. Watch for MessageError (publish as preview error state) and MessageEOS (publish as preview finished state). Use glib.MainLoop in a goroutine for dispatch.
- [x] Phase 1.4: Implement PreviewSession.Stop() using pipeline.SendEvent(gst.NewEOSEvent()) + pipeline.BlockSetState(gst.StateNull). Replace the current stdin 'q' + close approach.
- [x] Phase 1.5: Handle all source types: display (ximagesrc with startx/starty/endx/endy), region (ximagesrc with rect), window (ximagesrc with show-cursor), camera (v4l2src with device path and optional size). Map dsl.EffectiveVideoSource.Type to the correct source element and properties.
- [x] Phase 1.6: Test GStreamer preview end-to-end: display preview, camera preview, region preview. Verify preview suspend/restore still works via PreviewManager. Verify MJPEG HTTP streaming to browser still works (PreviewManager already handles this from stored frames).
- [x] Phase 2.1: Implement GStreamerRecordingRuntime in pkg/media/gst/recording.go. Build video recording pipelines: one GStreamer pipeline per VideoJob. ximagesrc/v4l2src -> videoconvert -> capsfilter(resolution/fps) -> x264enc(zerolatency,veryfast) -> mp4mux/qtmux -> filesink. Based on validated experiment in scripts/03-gst-launch-preview-x11.sh test 2.
- [ ] Phase 2.2: Build audio recording pipelines. For each AudioMixJob: create N pulsesrc elements -> volume elements -> audiomixer -> audioconvert -> wavenc/opusenc -> filesink. Based on validated experiment in scripts/04-gst-launch-audio-capture.sh and scripts/07-go-gst-audio-capture/.
- [ ] Phase 2.3: Implement RecordingSession.Stop() with graceful EOS: send EOS to all pipelines, wait for files to be finalized (muxer headers written), then set pipelines to NULL. This replaces the current 'write q to stdin and wait' approach.
- [ ] Phase 2.4: Map recording session states (starting, running, stopping, finished, failed) and events (process_started, process_log, state_changed) from GStreamer bus messages to the existing SessionManager expectations. The SessionManager should not need changes.
- [ ] Phase 2.5: Handle max-duration timeout. Use a Go timer that sends EOS to the recording pipeline when the max duration is reached. GStreamer pipelines don't have a built-in time limit, so this must be done in Go.
- [ ] Phase 2.6: Test recording end-to-end: start recording, verify MP4 output is valid (ffprobe), verify WAV output is valid, test stop recording, test cancel recording, test max-duration timeout, test recording with active previews.
- [ ] Phase 3.1: Add TakeScreenshot(ctx) ([]byte, error) to PreviewSession interface. Simplest implementation: return the latest JPEG frame already stored by the preview appsink. No extra pipeline needed for V1.
- [ ] Phase 3.2: Add HTTP endpoint GET /api/previews/{id}/screenshot that calls TakeScreenshot() and returns the JPEG bytes with appropriate Content-Type. Update proto/screencast/studio/v1/web.proto if a structured response is preferred.
- [ ] Phase 3.3: Add live audio effect elements to the recording pipeline. Insert volume and audiodynamic elements between pulsesrc and the encoder. These must be created during pipeline construction and stored as references on the recording session for runtime adjustment.
- [ ] Phase 3.4: Add HTTP endpoint POST /api/audio/effects with JSON body {gain: float64, compressor_enabled: bool, ...}. The handler calls RecordingSession.SetAudioGain() etc., which sets properties on the live GStreamer elements via element.Set(). Changes take effect on the next buffer.
- [ ] Phase 3.5: Add level element to the audio pipeline for live VU meter. Set level interval=50ms, post-messages=true. Parse bus MessageElement messages to extract dB values and publish them via WebSocket as a new event type (audio_level).
- [ ] Phase 3.6: Test screenshots during recording, test live gain adjustment during recording, test VU meter data flowing to browser.
- [ ] Phase 4.1: Introduce a capture graph registry (e.g., CaptureRegistry keyed by source signature). When a preview or recording needs a source, check if a live capture pipeline already exists for that source. If yes, add a new branch to its tee. If no, create a new capture pipeline with a tee.
- [ ] Phase 4.2: Remove the PreviewManager.SuspendAll / RestoreSuspended workaround. Preview and recording now share the same capture source via tee, so suspension is no longer needed.
- [ ] Phase 4.3: Delete pkg/recording/ffmpeg.go, pkg/media/ffmpeg/, internal/web/preview_runner.go. Remove FFmpeg from system dependencies. The entire FFmpeg code path should be gone.
- [ ] Phase 4.4: Clean up internal/web/server.go to remove preview handoff logic (the code that suspends previews before recording and restores after). Verify all tests pass without FFmpeg installed.
- [ ] Phase 5.1: Add transcription tee branch to the audio recording pipeline. After audiomixer, add a tee: branch 1 goes to wavenc/filesink (recording), branch 2 goes to capsfilter(16kHz/mono) -> appsink (transcription feed).
- [ ] Phase 5.2: Implement Go-side audio chunking. In the transcription appsink callback, accumulate PCM buffers. Every 3 seconds of audio (16000*2*3 = 96000 bytes for 16kHz 16-bit mono), flush the buffer and pass to the transcription backend.
- [ ] Phase 5.3: Integrate with a transcription backend. Start with the simplest option: invoke local whisper CLI as a subprocess, pass the audio chunk as a temp WAV file, read the text output. Publish transcription segments via WebSocket.
- [ ] Phase 5.4: Add WebSocket event type for transcription segments. Add browser UI to display live transcription text alongside the recording controls. Consider showing timestamps per segment.
- [ ] Phase 5.5: Test live transcription end-to-end: start recording with transcription enabled, speak into mic, verify text appears in browser in near-real-time (3-5 second delay is acceptable for V1).
- [ ] Stretch goal: Greenscreen compositing. Use the alpha element (method=green/blue/custom, angle, noise-level) to chroma-key a webcam source, then use compositor to overlay it on the screen capture. Pipeline: [ximagesrc -> compositor sink_0] + [v4l2src -> alpha(method=green) -> compositor sink_1 xpos/Y/width/height] -> encoder -> output. See scripts/08-gst-launch-greenscreen/ for validated proof of concept.
- [ ] Stretch goal: Greenscreen UI controls. Add DSL fields for greenscreen settings (chroma_key_color, chroma_key_angle, chroma_key_noise_level, overlay_x, overlay_y, overlay_width, overlay_height). Add HTTP endpoint to adjust these live during recording. The alpha and compositor element properties are all controllable at runtime.
- [ ] Cross-cutting: Write integration tests for each new pipeline type. Use fake GStreamer sources (videotestsrc, audiotestsrc) and appsinks instead of real hardware. Validate that pipeline construction, frame capture, EOS handling, and error propagation all work without needing a display or microphone.
- [ ] Cross-cutting: Document the go-gst lifetime management patterns to avoid the double-free bug observed in v1.4.0 (see diary Step 5). Rule: never call GetMetadata on a factory after creating an element from it. If more CGO memory issues surface, evaluate fallback to a C helper process communicating via Unix sockets.
- [ ] Cross-cutting: Update go.mod to add github.com/go-gst/go-gst and github.com/go-gst/go-glib dependencies. Document the CGO build requirements (libgstreamer1.0-dev, libgstreamer-plugins-base1.0-dev) in README.md and CI configuration.
- [ ] Cross-cutting: Update CI (GitHub Actions) to install GStreamer dev headers before go build. Add apt-get install libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev to the build step. Expect ~60-90 second CGO compilation time per build.
